#!/usr/bin/with-contenv sh

if [ ! -z "${NGINX_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/nginx/${NGINX_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
elif [ ! -z "${NGINX_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/nginx/${NGINX_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
fi
