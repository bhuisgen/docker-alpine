#!/usr/bin/with-contenv sh

if [ ! -z "${RUNDECK_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rundeck/${RUNDECK_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${RUNDECK_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rundeck/${RUNDECK_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
