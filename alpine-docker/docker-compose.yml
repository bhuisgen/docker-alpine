version: '3.8'

services:
    docker-compose:
        image: bhuisgen/alpine-docker
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - /var/run/docker.sock:/var/run/docker.sock
            - ./:/data
