#!/usr/bin/with-contenv sh

if [ ! -z "${MARIADB_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/mariadb/${MARIADB_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${MARIADB_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/mariadb/${MARIADB_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
