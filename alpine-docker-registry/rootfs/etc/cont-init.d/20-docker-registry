#!/usr/bin/with-contenv sh

if [ ! -z "${REGISTRY_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/docker-registry/${REGISTRY_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${REGISTRY_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/docker-registry/${REGISTRY_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
