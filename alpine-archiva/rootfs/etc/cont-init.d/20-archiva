#!/usr/bin/with-contenv sh

if [ ! -z "${ARCHIVA_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/archiva/${ARCHIVA_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${ARCHIVA_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/archiva/${ARCHIVA_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
