version: '3.7'

services:
    xvfb:
        image: bhuisgen/alpine-xvfb
        ports:
            - "99:99"
        volumes:
            - /etc/localtime:/etc/localtime:ro
        environment:
            #- XVFB_TEMPLATE=generic                 # configuration template

            #
            # template generic
            #
