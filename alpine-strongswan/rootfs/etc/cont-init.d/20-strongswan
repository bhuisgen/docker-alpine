#!/usr/bin/with-contenv sh

if [ ! -z "${STRONGSWAN_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/strongswan/${STRONGSWAN_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${STRONGSWAN_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/strongswan/${STRONGSWAN_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
