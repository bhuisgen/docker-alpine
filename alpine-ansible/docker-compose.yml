version: '3.2'

services:
    ansible:
        image: bhuisgen/alpine-ansible
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - ./:/data
            - $HOME/.ssh:/root/.ssh
