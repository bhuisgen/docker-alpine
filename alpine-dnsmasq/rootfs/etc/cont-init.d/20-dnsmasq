#!/usr/bin/with-contenv sh

if [ ! -z "${DNSMASQ_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/dnsmasq/${DNSMASQ_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${DNSMASQ_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/dnsmasq/${DNSMASQ_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
