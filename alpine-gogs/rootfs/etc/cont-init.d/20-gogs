#!/usr/bin/with-contenv sh

if [ ! -z "${GOGS_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/gogs/${GOGS_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${GOGS_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/gogs/${GOGS_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
