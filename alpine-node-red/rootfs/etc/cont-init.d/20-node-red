#!/usr/bin/with-contenv sh

if [ ! -z "${NODERED_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/nodered/${NODERED_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${NODERED_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/nodered/${NODERED_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
