#!/usr/bin/with-contenv sh

if [ ! -z "${RSYSLOG_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rsyslog/${RSYSLOG_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${RSYSLOG_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rsyslog/${RSYSLOG_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
