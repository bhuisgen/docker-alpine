#!/usr/bin/with-contenv sh

if [ ! -z "${PORTAINER_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/portainer/${PORTAINER_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${PORTAINER_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/portainer/${PORTAINER_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
