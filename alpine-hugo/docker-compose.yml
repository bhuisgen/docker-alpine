version: '3.8'

services:
    hugo:
        image: bhuisgen/alpine-hugo
        ports:
            - "1313:1313"
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - ./:/data/
