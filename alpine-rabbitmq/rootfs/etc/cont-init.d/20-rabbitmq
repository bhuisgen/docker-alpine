#!/usr/bin/with-contenv sh

if [ ! -z "${RABBITMQ_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rabbitmq/${RABBITMQ_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${RABBITMQ_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/rabbitmq/${RABBITMQ_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
