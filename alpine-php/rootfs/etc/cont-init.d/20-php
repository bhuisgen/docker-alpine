#!/usr/bin/with-contenv sh

if [ ! -z "${PHP_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/php/${PHP_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${PHP_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/php/${PHP_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
