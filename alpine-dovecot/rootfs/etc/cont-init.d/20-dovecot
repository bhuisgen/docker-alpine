#!/usr/bin/with-contenv sh

if [ ! -z "${DOVECOT_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/dovecot/${DOVECOT_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${DOVECOT_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/dovecot/${DOVECOT_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
