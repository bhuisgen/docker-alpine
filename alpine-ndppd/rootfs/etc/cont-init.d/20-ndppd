#!/usr/bin/with-contenv sh

if [ ! -z "${NDPPD_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/ndppd/${NDPPD_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${NDPPD_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/ndppd/${NDPPD_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
