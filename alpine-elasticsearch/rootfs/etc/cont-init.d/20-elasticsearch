#!/usr/bin/with-contenv sh

if [ ! -z "${ELASTICSEARCH_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/elasticsearch/${ELASTICSEARCH_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${ELASTICSEARCH_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/elasticsearch/${ELASTICSEARCH_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
