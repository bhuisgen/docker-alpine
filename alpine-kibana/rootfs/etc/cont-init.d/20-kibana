#!/usr/bin/with-contenv sh

if [ ! -z "${KIBANA_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/kibana/${KIBANA_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${KIBANA_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/kibana/${KIBANA_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
