#!/usr/bin/with-contenv sh

if [ ! -z "${KAPACITOR_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/kapacitor/${KAPACITOR_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${KAPACITOR_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/kapacitor/${KAPACITOR_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
