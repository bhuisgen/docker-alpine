version: '3.8'

services:
    python:
        image: bhuisgen/alpine-python
        volumes:
            - /etc/localtime:/etc/localtime:ro
        environment:
            - ENV=local                             # environment name
