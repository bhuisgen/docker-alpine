#!/usr/bin/with-contenv sh

if [ ! -z "${HOSTAPD_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/hostapd/${HOSTAPD_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${HOSTAPD_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/hostapd/${HOSTAPD_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
