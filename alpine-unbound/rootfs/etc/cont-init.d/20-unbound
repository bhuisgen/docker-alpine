#!/usr/bin/with-contenv sh

if [ ! -z "${UNBOUND_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/unbound/${UNBOUND_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${UNBOUND_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/unbound/${UNBOUND_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
