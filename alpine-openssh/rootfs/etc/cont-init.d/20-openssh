#!/usr/bin/with-contenv sh

if [ ! -z "${OPENSSH_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/openssh/${OPENSSH_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${OPENSSH_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/openssh/${OPENSSH_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
