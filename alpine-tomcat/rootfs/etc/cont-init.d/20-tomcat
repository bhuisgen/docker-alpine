#!/usr/bin/with-contenv sh

if [ ! -z "${TOMCAT_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/tomcat/${TOMCAT_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${TOMCAT_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/tomcat/${TOMCAT_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
