#!/usr/bin/with-contenv sh

if [ ! -z "${INFLUXDB_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/influxdb/${INFLUXDB_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${INFLUXDB_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/influxdb/${INFLUXDB_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
