#!/usr/bin/with-contenv sh

if [ ! -z "${GIT_TEMPLATE}" ] && [ -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/git/${GIT_TEMPLATE}"/*.hcl /etc/container.d/conf-init.d/
elif [ ! -z "${GIT_TEMPLATE}" ] && [ ! -z "${CONSUL_ADDR}" ]; then
    cp "/etc/container.d/templates/git/${GIT_TEMPLATE}"/*.hcl /etc/container.d/conf-services.d/
fi
